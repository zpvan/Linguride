# Bindings

This directory is reserved for future platform bindings once the desktop-first
Rust core is stable.

Current Phase 5 policy:

- Desktop is the first and only Rust core consumer.
- Browser runtime stays TypeScript-only.
- iOS and Android bindings are deferred until the Rust core and desktop flow are stable.
