Create a VS Code extension that can be installed locally. 
The extension is an AI agent whose one function is to analyze the difficulty of English articles. Specifically, it combines prompt text with portions of the article and sends them to DeepSeek’s LLM to assess the article’s difficulty.